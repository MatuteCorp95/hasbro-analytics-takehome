name: 'hasbro_analytics'
version: '1.0.0'
config-version: 2

profile: 'hasbro_analytics'

model-paths: ["models"]
seed-paths: ["seeds"]
test-paths: ["tests"]
macro-paths: ["macros"]
snapshot-paths: ["snapshots"]
analysis-paths: ["analyses"]

target-path: "target"
clean-targets:
  - "target"
  - "dbt_packages"

models:
  hasbro_analytics:
    staging:
      +materialized: view
    marts:
      +materialized: table